<div>
    <nav>
        <h1>Client Dash</h1>
        <div>
            <button class="main-button">Welcome {{user.first_name}}</button>
            <button class="main-button">Log Out</button>
        </div>
    </nav>

    <div class="container">
        <h1>Schedule</h1>

        <div class="buttons-container">
            <button class="main-button">Boarding</button>
            <button class="main-button">Training</button>
            <button class="main-button">Daycare</button>
        </div>

        <h2>My Dogs</h2>
        {{#each user.dog as |dog|}}
       <p>{{dog.name}}</p>
       {{/each}}
        <button id="addDogButton" class="add-dog-button">Add new dog</button>

        <div id="dogContainer"></div>
    </div>
</div>
    <script src="./public/js/homepage.js"></script>